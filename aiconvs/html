这是一个html文件模板

```
<!DOCTYPE html>
<html>
<head>
<title>文章归档</title>
<meta charset="utf-8">
<link rel="stylesheet" href="/style3.css">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>
<body>
<pre class="back"><a href="../">../</a></pre>
<!--TODO-->
<hr>
<p id="email">Email: i (at) mistivia (dot) com</p>
<script>
var emailElement = document.getElementById('email');
var base64String = "RW1haWw6IGlAbWlzdGl2aWEuY29tCg==";
var decodedString = atob(base64String);
emailElement.innerHTML = decodedString;
</script>
</body>
<script data-goatcounter="https://blog.mistivia.com:8081/count"
        async src="https://blog.mistivia.com:8081/count.js"></script>
</html>
```

在TODO处添加pdf.js显示一个pdf文件。不需要太复杂，能用mozilla的pdf.js的default viewer就行。

---
要在你的 HTML 模板中使用 **Mozilla PDF.js 的 Default Viewer（默认查看器）**，最简单且标准的方法是通过 `<iframe>` 嵌入。

PDF.js 的“默认查看器”是一个包含完整 UI（工具栏、缩放、翻页等）的 Web 应用程序，它不仅仅是一个 JS 文件，而是一整套 HTML/CSS/JS 资源。

### 解决方案

#### 1. 修改后的 HTML 代码

我已在 `TODO` 处添加了嵌入代码。

```html
<!DOCTYPE html>
<html>
<head>
<title>文章归档</title>
<meta charset="utf-8">
<link rel="stylesheet" href="/style3.css">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<style>
    /* 添加简单的样式让阅读器适应屏幕 */
    .pdf-wrapper {
        width: 100%;
        height: 80vh; /* 占据视口高度的80% */
        border: 1px solid #ccc;
        margin-bottom: 20px;
    }
    .pdf-wrapper iframe {
        width: 100%;
        height: 100%;
        border: none;
        display: block;
    }
</style>
</head>
<body>
<pre class="back"><a href="../">../</a></pre>

<!-- PDF.js Viewer Start -->
<div class="pdf-wrapper">
    <!-- 
      注意：
      1. src 指向 viewer.html 的路径
      2. ?file= 后面跟你的 PDF 文件路径 
    -->
    <iframe src="/pdfjs/web/viewer.html?file=/path/to/your/document.pdf">
        <p>您的浏览器不支持 iframe，请<a href="/path/to/your/document.pdf">点击此处下载 PDF</a>。</p>
    </iframe>
</div>
<!-- PDF.js Viewer End -->

<hr>
<p id="email">Email: i (at) mistivia (dot) com</p>
<script>
var emailElement = document.getElementById('email');
var base64String = "RW1haWw6IGlAbWlzdGl2aWEuY29tCg==";
var decodedString = atob(base64String);
emailElement.innerHTML = decodedString;
</script>
</body>
<script data-goatcounter="https://blog.mistivia.com:8081/count"
        async src="https://blog.mistivia.com:8081/count.js"></script>
</html>
```

---

### 2. 关键步骤（必读）

因为你要用的是 **Default Viewer**（带界面的那个），你需要下载 PDF.js 的构建包并放到你的服务器上。你不能只引入一个 JS 链接就得到完整的 UI。

**操作步骤：**

1.  **下载构建包**：
    访问 [PDF.js GitHub Releases](https://github.com/mozilla/pdf.js/releases)，下载最新的 **`pdfjs-*-dist.zip`** (通常叫 Prebuilt)。

2.  **解压并上传**：
    解压后你会看到 `build/` 和 `web/` 两个文件夹。将这两个文件夹上传到你的网站目录中（例如放在 `/pdfjs/` 目录下）。
    
    目录结构应该类似这样：
    ```text
    /your-website/
    ├── style3.css
    ├── article.html (你的这个模板文件)
    ├── your-file.pdf (你要显示的PDF)
    └── pdfjs/
        ├── build/
        │   └── pdf.js
        └── web/
            ├── viewer.html  <-- iframe 的 src 指向这里
            ├── viewer.css
            └── viewer.js
    ```

3.  **修改路径**：
    在上面的 HTML 代码中，修改 `src` 属性：
    `src="/pdfjs/web/viewer.html?file=/your-file.pdf"`

    *   `/pdfjs/web/viewer.html`: 刚才上传的 viewer 文件的路径。
    *   `file=/your-file.pdf`: 你想展示的 PDF 文件的路径。

### 为什么这样做？
这是使用 PDF.js 最稳定、兼容性最好的方式。它利用了 Mozilla 官方写好的查看器界面，支持缩放、打印、侧边栏预览等功能，而且不需要你自己写复杂的 Canvas 渲染代码。
